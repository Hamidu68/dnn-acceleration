#include <iostream>
#include <ap_int.h>
#include <hls_stream.h>

typedef ${Data_type} DATA_T;

${sw_def_layer}


void ${model_name}${post0}(${func_params}) {
	${sw_output_variables}

	${sw_call_layer}
}
