for (m = 0; m <  {Output_channel} ; m++) {{
	for (k = 0; k < {Input_channel} ; k++) {{
		fread(&W{line_number}[m][k], sizeof(DATA_IN), 1, w_stream);
	}}
    {comment}fread(&B{line_number}[m], sizeof(DATA_IN), 1, b_stream);
}}
