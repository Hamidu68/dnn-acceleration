show_result= show_result + "$Name" + " : [[";
for (k = 0; k <  $Output_channel ; k++) {
	show_result += "[";
	for (x = 0; x < $Output_width ; x++) {
		show_result += "[";
		for(y = 0; y < $Output_height ; y++) {
			show_result = show_result + " " + to_string((int)(O${line_number}_SW[k][x][y]));
		}
		show_result += "]";
		if(x != $Output_width -1 )
			show_result += "\n   ";
	}
	if(k != $Output_channel -1 )
		show_result += "]\n\n  ";
}
show_result += "]]]\n\n";
